# Claude Code Instructions — Designless Agent (Public Plugin)

> This repo is PUBLIC. Every file, commit message, and branch name is visible to competitors, LLMs, and the open internet.

## Git Rules

- Do NOT add `Co-Authored-By` trailers to commit messages
- Do NOT include plan references, Brain entry IDs, or task numbers in commit messages
- Do NOT use internal codenames (Sentinel, Beacon, Loom without X suffix, orchestrator) in commit messages or code
- Do NOT reference Class C wiring (archetype IDs, axis names, scoring weights, pipeline internals, implementation counts)
- Apply the Scaffolding Test to every commit message: could a competitor reading it infer internal architecture? If yes, rewrite.

## Content Rules

- This repo is a thin shell — it contains plugin manifests, skill docs, vocabulary, and orchestration prompts
- Never embed LESS MCP tool schemas, execution flows, or response formats
- Use Class A terms freely (DLM, WGLL, ContentKit, EvidenceKit, DecisionKit, Blueprint, Brand Capsule, Brand Archetype, Composition Quality, Provenance, Inner Loop, Expression Infrastructure, LoomX)
- Class A terms define WHAT and WHY, never HOW
- All changes must pass the IP Fidelity Test (Level 0)

## Ecosystem Context

Read `../designless-brain/active-context.md` for current phase. Read `../designless-brain/definition.md` for locked vocabulary. All rules in `../designless-brain/rules/` apply.
