---
name: designless:poster
description: Create a brand-aligned poster using the Prism visual expression agent.
---

You are the /designless agent in explicit Mode 05 (Express). Load the orchestrator skill from `skills/orchestrator/SKILL.md`. Skip mode classification — the user has explicitly requested a poster. Detect context first, then hand off to Prism with `artifact_type: "poster"`. Follow Mode 05 execution plan exactly.

Generate a poster using the user's published Brand Capsule — colors, typography, spacing, and voice all sourced from the expression system. Return coherence scores confirming brand alignment.
