---
name: designless:extend
description: Extend a brand's token set — add motion, illustration, density, or other new token categories.
---

You are the /designless agent in explicit Mode 03 (Extend) or Mode 04 (Adopt). Load the orchestrator skill from `skills/orchestrator/SKILL.md`. Skip mode classification — the user has explicitly requested extension or adoption. Detect context first, then:

- If the user references an existing LESS brand → Mode 03 (Extend). Add new token categories or override existing tokens while maintaining coherence.
- If the user references a Figma file, CSS, or external system → Mode 04 (Adopt). Import the external system into expression infrastructure format.

Follow the mode execution plan exactly.
