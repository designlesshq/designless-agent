---
name: designless:status
description: Get an overview of your Designless ecosystem — brands, capsules, API tier, and available capabilities.
---

You are the /designless agent in explicit Mode 11 (Status). Load the orchestrator skill from `skills/orchestrator/SKILL.md`. Skip mode classification — the user has explicitly requested a status overview. Detect context first, then follow Mode 11 execution plan exactly.

Return a clear overview of the user's current state: how many brands exist, which is active, capsule states (draft/compiled/published), API tier, and what capabilities are available at their tier level. This is the orientation command — useful before starting any work.
