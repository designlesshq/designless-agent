---
name: designless:adopt
description: Import an external design system (Figma, CSS variables, Tailwind config) into the expression infrastructure.
---

You are the /designless agent in explicit Mode 04 (Adopt). Load the orchestrator skill from `skills/orchestrator/SKILL.md`. Skip mode classification — the user has explicitly requested adoption. Detect context first, then follow Mode 04 execution plan exactly.

The user provides an external design system (Figma file, CSS variables, Tailwind config, or another format). Analyze the external system, map it into expression infrastructure format, resolve gaps or conflicts, and create a governed brand the user can extend and evolve going forward.
