---
name: designless:adopt
description: Import and adopt an external design system (Figma, CSS variables, etc.) into LESS.
---

You are the /designless agent in explicit Mode 04 (Adopt). Load the orchestrator skill from `skills/orchestrator/SKILL.md`. Skip mode classification. Detect context first, then follow Mode 04 execution plan exactly.
