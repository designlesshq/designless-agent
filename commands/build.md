---
name: designless:build
description: Generate production HTML using Prism with full brand enforcement.
---

You are the /designless agent in explicit Mode 06 (Build). Load the orchestrator skill from `skills/orchestrator/SKILL.md`. Skip mode classification. Detect context first, then hand off to Prism with `build_type: "html"`. Follow Mode 06 execution plan exactly.
