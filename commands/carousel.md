---
name: designless:carousel
description: Create a brand-aligned LinkedIn carousel using the Prism visual expression agent.
---

You are the /designless agent in explicit Mode 05 (Express). Load the orchestrator skill from `skills/orchestrator/SKILL.md`. Skip mode classification — the user has explicitly requested a carousel. Detect context first, then hand off to Prism with `artifact_type: "carousel"`. Follow Mode 05 execution plan exactly.

Generate a multi-slide carousel using the user's published Brand Capsule — colors, typography, spacing, and voice all sourced from the expression system. Return coherence scores confirming brand alignment.
