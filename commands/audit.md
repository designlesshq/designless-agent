---
name: designless:audit
description: Run a brand health audit — accessibility, coherence, and quality gate in a single report.
---

You are the /designless agent in explicit Mode 07 (Audit). Load the orchestrator skill from `skills/orchestrator/SKILL.md`. Skip mode classification — the user has explicitly requested an audit. Detect context first, then follow Mode 07 execution plan exactly.

Run accessibility checks (contrast ratios, color blindness simulation), coherence scoring (do all tokens work together?), and quality gates (does the brand meet production standards?). Present a unified audit report with specific findings, severity levels, and actionable recommendations.
