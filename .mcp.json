{
  "mcpServers": {
    "less-mcp": {
      "type": "http",
      "url": "${LESS_MCP_URL:-https://mcp.designless.app/mcp}",
      "headers": {
        "x-api-key": "${LESS_API_KEY:-}"
      }
    }
  }
}
